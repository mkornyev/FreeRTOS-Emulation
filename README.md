# FreeRTOS Emulation Setup

###### Author: `mkornyev`

### Requirements 

* Vagrant / VirtualBox

### Setup

1. Start the VM with `vagrant up`
2. The entire setup is automated with the provided script: `vmSetup.sh`
